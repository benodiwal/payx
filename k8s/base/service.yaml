apiVersion: v1
kind: Service
metadata:
  name: payx-server
  namespace: payx
  labels:
    app.kubernetes.io/name: payx
    app.kubernetes.io/component: server
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: payx
    app.kubernetes.io/component: server
